top: VBox {
    text_a: Label {
	text: String "Label"
    }
    terminal_a: Terminal {
	width:	Int 	80
	height:	Int	25
	shell: Shell {
	}
    }
    button_a: Button {
        title:  String "Go to Second"
        pressed: Event() %{
		console.log("launch second view .. start") ;
		console.log("state (before) ... " + viewState.isActive) ;
		enterView("button_view") ;

		let retval = waitUntilActivate() ;
		console.log("state (after ) ... " + viewState.isActive) ;
		console.log("return value   ... " + retval) ;
        %}
    }
}

